#!/usr/bin/env python

import sys
import desispec.scripts.assemble_fibermap as assemble_fibermap

if __name__ == '__main__':
    args = assemble_fibermap.parse()
    sys.exit(assemble_fibermap.main(args))